export interface IconPickerSubmitDataType {
  icon: string
  color: string
  fontSize: number
}

export interface IconListType {
  iconData: string[]
  collection: string
  showText: boolean
  itemClass: string
  iconClass: string
  activeClass: string
}
